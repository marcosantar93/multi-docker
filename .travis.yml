sudo: required
language: generic 
services: 
    - docker

before_install:
    - docker -t marcosantarcangelo/react-test -f ./client/Dockerfile.dev ./client

script:
    - docker run -e CI=true USERNAME/react-test npm test
    